{
  "name": "hdl-board-server",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "license": "GPL-2.0-only",
  "description": "GHDL backend for HDLBoard — see ../docs/ghdl_implementation_plan.md",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/server.js",
    "dev": "tsc -b && node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.3"
  }
}
